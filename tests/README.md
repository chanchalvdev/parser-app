# tests/

Monorepo test matrix and strategy placeholders.

Planned subfolders:
- `apps/api/` for backend API tests
- `apps/worker/` for parser/extractor worker tests
- `apps/web/` for frontend interaction tests
